# NanoMDParser
A tiny Markdown parser using regex written in JavaScript with custom functions for span and div with classes including external links with the rel attribute added to the formatting syntax. Size? 3kb.
